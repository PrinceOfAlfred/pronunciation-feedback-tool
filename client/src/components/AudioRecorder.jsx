export const AudioRecorder = () => {
  return (
    <div>
      <button onClick={{}} disabled={false}>
        Start Recording
      </button>
      <button onClick={{}} disabled={false}>
        Stop Recording
      </button>
      <audio src={{}} controls />
    </div>
  );
};
